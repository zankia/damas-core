const assert = require('assert');
const subject = require('../../');

describe('index', () => {
    describe('main', () => {
        it('should run the server', () => {
            assert(subject);
        });
    });
});
